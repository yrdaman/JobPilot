[project]
name = "jobpilot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Rakesh Daman Yelakanti", email = "yrdaman196@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "chromadb>=1.5.9",
    "groq>=0.9.0",
    "pydantic>=2.0.0",
    "pypdf>=6.17.0",
    "streamlit",
]

[project.scripts]
jobpilot = "jobpilot:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false